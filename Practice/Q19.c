// Find the lcm of two numbers
#include <stdio.h>
int main()
{
    int n1, n2, lcm, i, max;
    printf("Enter two numbers: \n");
    scanf("%d%d", &n1, &n2);
    for (i = 0; i < n2; i++)
    {
        if (max = n1 < n2)

        {
            lcm = n2 * i;
            printf("%d", lcm);
        }
    }
}